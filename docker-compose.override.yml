services:
  nginx:
    volumes:
      - ./nginx/default.local.conf:/etc/nginx/conf.d/default.conf:ro

  backend:
    command: ["sleep", "infinity"]
    volumes:
      - ./backend:/app
      - ~/.ssh:/root/.ssh:ro
    working_dir: /app
